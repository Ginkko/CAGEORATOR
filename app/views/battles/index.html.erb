<h1>B A T T L E</h1>
<div class="row">
  <div class="col-md-4 col-md-offset-1">
    <h3 class="text-center"> <%= link_to @post1.title, post_path(@post1) %></h3>
    <hr>
    <img class="img-responsive" id="battle" src="<%= @post1.image %>"/>

    <%= form_tag("/battles", method: "update") do %>
      <%= hidden_field_tag :winning_post_id, @post1.id %>
      <%= hidden_field_tag :losing_post_id, @post2.id %>

      <%= submit_tag("V O T E") %>
    <% end %>

  </div>
  <div class="col-md-4 col-md-offset-1">
    <h3> <%= link_to @post2.title, post_path(@post2) %></h3>
    <hr>
    <img id="battle" src="<%= @post2.image %>"/>
    <%= form_tag("/battles", method: "update") do %>
      <%= hidden_field_tag :winning_post_id, @post2.id %>
      <%= hidden_field_tag :losing_post_id, @post1.id %>
      <%= submit_tag("V O T E") %>
    <% end %>
  </div>
</div>
<div class="row">
  <div class="col-md-4 col-md-offset-5"
    <h1>V S</h1>
  </div>
</div>
